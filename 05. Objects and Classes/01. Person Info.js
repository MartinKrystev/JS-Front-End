function personInfo(first, second, third) {
    let person = {};

    person.firstName = first;
    person.lastName = second;
    person.age = Number(third);

    return person;
}

// personInfo('Dark', 'Templar', 1732);
